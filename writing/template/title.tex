$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$if(subtitle)$
\subtitle{$subtitle$}
$endif$

$for(by-author)$
  \author{$by-author.name.literal$}
  $if(by-author.affiliations)$
    $for(by-author.affiliations)$
      \affil{%
        $if(by-author.affiliations.name)$
          $by-author.affiliations.name$
        $endif$
        $if(by-author.email)$\\\texttt{$by-author.email$}$endif$
      }
    $endfor$
  $else$
    $if(by-author.email)$
      \affil{\texttt{$by-author.email$}}
    $endif$
  $endif$
$endfor$

\date{$date$}

